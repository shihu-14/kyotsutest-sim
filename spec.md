# 共通テスト形式 ウェブ模試システム 要件定義書

## 1. システム概要
本システムは、$\LaTeX$を用いた高品質な見た目の作問機能と、実際のマーク模試をシミュレートした高い没入感を持つ受験・採点UIを提供するウェブアプリケーションである。

---

## 2. ユーザー機能（受験フェーズ）

### 2.1 試験選択・導入
- **問題セット選択**: 公開されている試験一覧から任意のセットを選択。
- **表紙（注意書き）ページ**: 
    - 試験時間、設問数、注意事項を表示。
    - 「試験開始」ボタン押下でカウントダウンを開始し、試験画面へ遷移。

### 2.2 試験実行（受験モード）
- **UIレイアウト**:
    - **左/中央エリア**: 問題冊子を再現。左右のナビゲーションによるページめくり。
    - **右エリア**: デジタルマークシートを表示。
- **双方向マーク同期**:
    - 問題文中のマーク選択肢をクリックすると、該当箇所が塗りつぶされる。
    - 同時に、右側のマークシートの該当番号も自動で塗りつぶされる。
    - マークシート側を直接クリックして回答することも可能（問題文側へも即時反映）。
    - クリックによる反転（ON/OFF）形式。複数選択解答にも対応。
- **タイマー機能**: 画面上部に残り時間を常時表示。制限時間終了で自動的に試験終了。
- **ナビゲーション**: マークシートの番号、または問題番号をクリックすることで、該当するページへ即座にジャンプする。

### 2.3 採点・結果演出（採点モード）
- **自動採点アニメーション**:
    - 第1問から順に、解答と正解を高速で走査。
    - 赤ペンで「○」または「×」が書き込まれるようなアニメーションを演出。
    - 正誤判定に合わせ、画面上の「現在の合計点」がリアルタイムに加算される。
- **結果表示**: 最終的な総合得点を画面中央に強調表示。

### 2.4 復習機能
- **解説表示**: 復習モードでは、間違えた問題の正解箇所を「赤色」で自動着色。
- **シームレス遷移**: 試験中と同様、マークシート経由で各問題へジャンプ可能。

---

## 3. 作問・管理機能（編集フェーズ）

### 3.1 $\LaTeX$ 編集エディタ
- **直接コーディング**: $\LaTeX$ コードをブラウザ上で直接入力・編集。
- **プレビュー**: 入力したコードをリアルタイムで共通テスト形式のフォント・レイアウトにレンダリング。
- **テンプレート機能**:
    - 大問・小問のタイトルフォント設定。
    - 共通テスト特有の「ア」「イ」などのマーク用囲み文字の自動生成。

### 3.2 マーク・ロジック自動付与
- **マークコマンドの自動解析**:
    - $\LaTeX$ コード内に特定のマーク用コマンド（例：`\mark{ア}`）を記述すると、システムが自動で「解答欄」と認識。
    - 右側のデジタルマークシートに該当するスロットが自動生成される。
    - 正解値と配点をコード内（または設定パネル）で指定することで、採点ロジックも自動構成される。

### 3.3 画像・図形管理
- **TikZ/画像対応**: 図形は基本的に $\LaTeX$ の `TikZ` コード、または外部画像の埋め込みコードで記述。

---

## 4. 技術要件（暫定）

- **レンダリング**:
    - 数式: `KaTeX` または `MathJax`
    - 図形（TikZ）: `TikZJax` 等を用いたクライアントサイド描画
- **ステート管理**: React, Vue, または Svelte 等によるリアクティブな状態管理
- **アニメーション**: SVGアニメーションまたは Canvas (GSAP, Framer Motion等)
- **データ構造**: 問題データおよび解答データは JSON 形式で保持

---

## 5. その他
- 共通テスト特有のフォント（教科書体に近いもの）の導入。
- オフライン環境での解答保持ロジック。
- $\LaTeX$ コードを自動生成するGUIエディタ（ドラッグ＆ドロップ操作）の拡張。


## 6. 推奨される技術スタックとシステムアーキテクチャ

本システムの核心である「問題とマークシートのリアルタイム同期」「高速なアニメーション採点」「ウェブ上でのLaTeXレンダリング」を遅延なく実現するためには、以下の技術選定が適切である。

### 6.1 フロントエンド（ユーザーインターフェースと状態管理）
- **UIフレームワーク: Next.js (React)**
  - **選定理由:** 「問題文コンポーネント」と「マークシートコンポーネント」を独立させつつ連携させるコンポーネント指向の開発に最適。また、ページ遷移の最適化機能により、大量の画像やテキストを含む試験画面でも高速なロードが可能。
- **状態管理: Zustand または Jotai**
  - **選定理由:** ユーザーが問題文の選択肢をクリックした際、離れた場所にあるマークシートのUIへ状態変化をミリ秒単位で同期させる必要がある。Reduxよりも記述が軽量で、レンダリングの最適化（無駄な再描画の防止）が容易なこれらのライブラリが適している。

### 6.2 LaTeX・数式・図形のレンダリング技術
- **数式レンダリング: KaTeX**
  - **選定理由:** ブラウザ上で数式を描画する標準的なライブラリ（MathJax等）の中でも、描画速度が圧倒的に高速。作問時のリアルタイムプレビューにおいて、入力遅延を感じさせないUIを実現するために必須である。
- **図形描画 (TikZ等): TikZJax (WebAssembly)**
  - **選定理由:** 従来はサーバー側で画像にコンパイルする必要があったTikZコードを、WebAssembly技術を用いてブラウザ側（クライアントサイド）で直接SVG画像として高速変換する。サーバー負荷を大幅に削減し、リアルタイム表示を実現できる。

### 6.3 演出・アニメーション技術
- **採点アニメーション: GSAP (GreenSock Animation Platform) または Framer Motion**
  - **選定理由:** 「上から下へ流れるような高速な赤ペンでの丸付け」「現在の総合得点のカウントアップ」といった、連続的でタイミング制御がシビアなアニメーションを60fpsで滑らかに実行するため。CSSアニメーション単体では実装が困難な複雑なシーケンス制御が可能。

### 6.4 バックエンド・データ管理
- **BaaS (Backend as a Service): Supabase または Firebase**
  - **選定理由:** 開発初期のインフラ構築コストを抑えつつ、認証機能やデータベースを迅速に導入できる。特にSupabaseは内部がPostgreSQLであるため、問題データ（LaTeX文字列や配点情報）を柔軟なJSONB形式で格納しつつ、厳密なリレーション管理も両立できる。
- **リアルタイム同期機構: WebSockets**
  - **選定理由:** 万が一試験中にブラウザがクラッシュした場合でも、1マークごとの解答状況をサーバーにリアルタイムで自動保存（オートセーブ）し、試験の安全性を担保するために使用する。

### 6.5 作問エディタ実装
- **コードエディタUI: Monaco Editor**
  - **選定理由:** VS Codeの基盤技術であり、ウェブブラウザ上に直接組み込める。作問者に対して、LaTeXコードのシンタックスハイライト（構文の色分け）や入力補完機能を標準で提供できるため、開発コストを下げつつ高いユーザー体験を提供可能。
